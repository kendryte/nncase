name: .NET

on:
  push:
    branches: [ feature/rebuild-ir ]
  pull_request:
    branches: [ feature/rebuild-ir ]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-18.04] #,windows-2019,macos-10.15]

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      env: 
        LD_LIBRARY_PATH: "${{github.workspace}}/src/Nncase.Tests/bin/Debug/net6.0/runtimes/linux-x64/native;${{github.workspace}}/src/Nncase.Cli/bin/Debug/net6.0/runtimes/linux-x64/native"
      run: dotnet test --no-build --verbosity normal